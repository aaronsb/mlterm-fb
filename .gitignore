# build outputs
out/
*.pkg.tar.zst
*.pkg.tar.xz

# AUR publishing clone (managed by `make publish`)
.aur/
.SRCINFO.tmp
# makepkg work dirs (if building on host)
src/
pkg/
mlterm-*/
*.tar.gz

# logs
*.log
